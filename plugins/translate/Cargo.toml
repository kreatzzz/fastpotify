[package]
name = "woofer-plugin-translate"
version = "1.0.0"
edition = "2024"
rust-version = "1.95"
license = "MIT"
description = "The Translate plugin for Woofer: the reader's language under every lyric line"
repository = "https://github.com/kreatzzz/woofer-plugin-translate"
keywords = ["woofer", "plugin", "translate", "spotify"]
categories = ["multimedia::audio"]

[lib]
crate-type = ["cdylib"]

[dependencies]
woofer-plugin-sdk = { path = "../sdk" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
urlencoding = "2"

[dev-dependencies]
woofer-plugin-sdk = { path = "../sdk", features = ["harness"] }
# The tests read the query back out of the planned URLs.
urlencoding = "2"

[profile.release]
lto = "thin"
codegen-units = 1
strip = true

[workspace]
